# Enter your own customizations for the eth2 container here. These changes will persist after upgrades, so you only need to do them once.
#
# See https://docs.docker.com/compose/extends/#adding-and-overriding-configuration
# for more information on overriding specific parameters of docker-compose files.

version: "3.7"
services:
  eth2:
    x-rp-comment: Add your customizations below this line
    volumes:
      - {{ eth.secrets.dataDir }}/validators:/validators
      - {{ eth.eth2.dataDir }}:/ethclient
      - {{ rocketpool_user_home }}/.rocketpool/scripts:/setup:ro
