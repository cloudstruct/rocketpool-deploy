---
# Additional hosts to add to SSH port security group Allow
ssh_whitelist: []
# Example:
# ssh_whitelist:
#   - <your-ip-address>/32

# You can specify a public key here or leave it blank and AWS will generate one for you.
ssh_public_key: ''
